{
  "name": "deltarent-workspace",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev:mock": "node scripts/mock-server.mjs",
    "dev:web": "npm --prefix src/web run dev",
    "build:web": "npm --prefix src/web run build"
  }
}
