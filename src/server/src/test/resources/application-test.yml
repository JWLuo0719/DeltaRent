spring:
  datasource:
    url: jdbc:h2:mem:delta_trade_test;DB_CLOSE_DELAY=-1;MODE=MySQL
    username: sa
    password:
    driver-class-name: org.h2.Driver
  sql:
    init:
      mode: always
      data-locations: classpath:test-data.sql

mybatis:
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    com.jwluo0719.deltatrade: debug
